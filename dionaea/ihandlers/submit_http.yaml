- name: submit_http
  config:
      # the url to send the submission requests to
      url: "http://localhost:8080"
      # E-Mail (optional)
      # email: ""
      # username (optional)
      # user:
      # password (optional)
      # pass:
