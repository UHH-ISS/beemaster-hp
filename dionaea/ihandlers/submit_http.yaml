- name: submit_http
  config:
      # the url to send the submission requests to
      url: "http://localhost"
      # E-Mail (optional)
      # email: ""
      # username (optional)
      # user:
      # password (optional)
      # pass:
