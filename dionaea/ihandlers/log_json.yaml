- name: log_json
  config:
    # Uncomment next line to flatten object lists to work with ELK
    # flat_data: true
    handlers:
      # log to file or dev null or a host machine here
      - http://connector:8080
      #- file://@LOCALESTATEDIR@/dionaea/dionaea.json
